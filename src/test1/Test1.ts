// Unit test suite for SQRLStrategy code.

import { assert } from "chai";

describe('TestSuite1_1', () => {
  describe('simpleTests', () => {
    it('should assert a truism', () => {
      assert.equal(1, 1);
      
    });
  });
});
